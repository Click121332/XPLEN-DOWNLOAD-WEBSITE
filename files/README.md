# Local files

Place downloadable files in this directory and commit them with the website.

Use a relative path such as `/files/example.zip` when adding a file entry in `app.js`.

These files are public website assets. Do not place private or sensitive files here.
